% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/db_con.R
\name{db_con}
\alias{db_con}
\title{Create Database Connection or Connection Pool}
\usage{
db_con(
  driver = RPostgres::Postgres(),
  port = 5432,
  dbname = "forschung",
  host = "10.82.152.222",
  as_pool = FALSE
)
}
\arguments{
\item{driver}{A DBI-compatible database driver.}

\item{port}{Integer. Database port.}

\item{dbname}{Character. Name of the database.}

\item{host}{Character. Database host address.}

\item{as_pool}{Logical. If \code{TRUE}, returns a connection pool via \pkg{pool}.}
}
\value{
A \code{DBIConnection} object or a \code{pool} object.
}
\description{
Establishes a database connection using stored environment credentials.
Automatically prompts for credentials if missing or invalid.
}
\details{
The function retries up to four times to establish a connection, prompting
the user for credentials if needed.
}
\examples{
\dontrun{
# Standard DBI connection
con <- db_con()

# Using a connection pool
pool <- db_con(as_pool = TRUE)

# Custom connection parameters
con <- db_con(
  driver = RPostgres::Postgres(),
  dbname = "forschung",
  host = "10.82.152.222"
)
}

}
